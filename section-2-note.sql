-- SQL Statement Fundamentals : 
SELECT FROM 
DISTINCT 
COUNT --  use * or 1 or column name match same result
WHERE, AND, OR, NOT
ORDER BY ASC DESC -- can use multiple column, default is ASC
LIMIT 
BETWEEN AND, IN
ILIKE, LIKE --  use with _ to match exactly 1 character and % to max 0 or multiple ones . LIKE case sensitive and ILIKE case insensitive

